{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel serve src/index.html --port 3000",
    "deploy": "rm -rf build && parcel build -d build --public-url /exampleCard/ src/index.html && gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "stylelint": "^13.10.0",
    "stylelint-config-standard": "^20.0.0"
  }
}
